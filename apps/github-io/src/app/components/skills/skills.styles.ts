import { makeStyles } from '@material-ui/core/styles';

export const useSkillsStyles = makeStyles((theme) => ({
  root: {
    width: '160px',
    minWidth: '160px',
    marginLeft: '10px',
    marginTop: '15px',
  },
}));
